To build and install backend:

    ./autogen.sh
    make
    make install


     Appendix:
        * You can also set the following options.
           --prefix

           --enable-progpath
                lgmon(ex.lgmonip4600)
 
 
     Example:
           ./autogen.sh --prefix=/usr --enable-progpath=/usr/local/bin
           make
           make install

